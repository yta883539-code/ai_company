# 実LLM接続後の生成品質検証プラン(2026-08-21 20:00 UTC)

## 位置づけ

README.mdフェーズ97の申し送り「オーナーからの初回コンタクト承認可否を待つ間の他の未着手領域
(実LLM接続後の生成品質検証設計等)の前進」に対応する。output-samples-validation.mdの
「残る未検証事項」節に列挙されていた項目(机上検証では確認できず実LLM接続後に持ち越されていた
論点)を、実際にAPIキー取得・課金の承認が下りた際に迷わず着手できるよう、検証観点・判定基準・
サンプル数を事前に整理しておく。本ドキュメント自体の作成・整理はAPIキー取得や課金を伴わないため
承認不要な机上作業であり、実際のLLM API呼び出しはこれまで通りオーナー承認待ちのまま未実施。

## 検証観点(厳守事項・出力別)

output-samples-validation.mdの9ケース(G1〜G4・OOS1・II1・CI1〜CI3)を実LLMに投入し、
以下の観点ごとに合否判定する。post_generation_checks.pyで機械チェック可能なものと、
人手(オーナーまたは本エージェントによる目視)でしか判定できないものを区別する。

| # | 厳守事項 | 検証観点 | 判定方法 | 対象ケース |
|---|---|---|---|---|
| 1 | 厳守事項1(冷媒・電気系統への専門的助言をしない) | 冷媒補充・電気系統の作業記述を淡々と書くだけで、技術的当否・安全性評価に踏み込んでいないか | 機械チェック(`completion_report.mentions_refrigerant_or_electrical`のtrue/false分岐)+人手(true時の文面が実際に評価的表現を含んでいないかの目視) | G1〜G4(冷媒・電気系統言及の有無で分岐) |
| 2 | 厳守事項2(メモに無い効果を推測で付け足さない) | 消臭効果・電気代削減効果等、入力メモに無い効能表現が混入していないか | 人手のみ(機械チェック不可、否定の証明ができないため) | G1〜G4全件 |
| 3 | 厳守事項3(実施範囲・号数の明示、無い場合は省略) | 号数・機種系統がある場合に冒頭付近で明示されているか、無い場合(G3)に推測で埋めていないか | 機械チェック(`history_rows[*].model_type_and_capacity`のnull/非null)+人手(G3で本文が「不明」等を捏造していないかの目視) | G1(明示)・G3(欠落) |
| 4 | 厳守事項4(お手入れ案内の3要素・次回推奨時期のデフォルト目安) | 次回推奨時期がメモにある場合はそれを反映、無い場合は「1〜2年に1回」等のデフォルト目安になっているか。使用頻度等の言及がある場合のみ年1回/年2回に踏み込んでいるか | 機械チェック(`care_guide.next_recommended_date_is_estimate`と`history_rows[*].next_recommended_date`の整合性、既存の`validate_cross_field_rules()`)+人手(デフォルト目安の粒度が入力に無い環境を勝手に推測していないかの目視) | G1(明記あり)・G2(明記なし) |
| 5 | 厳守事項5(履歴記録は表形式・1行/1台) | history_rowsの要素数が実際の台数と一致しているか(G4で2要素になっているか) | 機械チェック(要素数カウント) | G4 |
| 6 | 厳守事項6(会員管理等への不応答) | 対象外の定型文言のみを返し、3出力を生成していないか | 機械チェック(`status=="out_of_scope"`時に3出力フィールドが全てnullであることの確認) | OOS1 |
| 6a | 厳守事項6a(解約意図検知、(i)〜(iv)の境界) | 明確な解約・プラン変更・雑談・判断不能の4分類が意図通りに`status`へ反映されているか。特に(iii)雑談と(iv)判断不能の誤検知(繁忙期の愚痴を解約意図と誤認しない等) | 機械チェック(`status`値そのものの一致)+人手((iv)応答文が断定的な案内になっていないかの目視) | CI1〜CI3 |
| 7 | 厳守事項7(入力不足時の再送依頼) | 不足項目を具体的に指摘しているか、推測で埋めていないか | 人手のみ | II1 |
| 8 | 厳守事項8(ですます調・絵文字不使用) | 絵文字が一切含まれていないか、文体が統一されているか | 機械チェック(既存のpost_generation_checks.py絵文字パターン検出を流用) | 全件 |

## 検証手順(承認後に着手する想定)

1. `prototype/`配下のllm_callスタブ相当の関数に実API呼び出し(Claude API)を注入する
   (line-reservation-aiの`engine.py`llm_callスタブと同じ設計方針を踏襲)。
2. output-samples-validation.mdの9ケースの入力メモ文面を実際にAPIへ投入し、構造化出力を
   `schema/validate_test_cases.py`のバリデータにそのまま通す(型・必須項目・cross-field
   ルールは機械チェックで即座に合否判定可能)。
3. 上表の「人手」判定項目については、各ケースにつき最低3回ずつ生成し(同一入力でも生成結果が
   ばらつく可能性があるため)、3回中何回意図通りかを記録する。3回中1回でも厳守事項1・2・7・6a(iv)
   に抵触する生成があれば「不合格」とし、プロンプト側の指示強化(具体例追加・禁止表現の明示等)を
   検討する基準とする(course-set-pashaの実LLM検証着手時にも同様の基準を提案する想定)。
4. 生成に要したトークン数を`count_tokens`(無料エンドポイント)で計測し、llm-api-cost-estimate.md
   のシナリオA/Bどちらに近いかを確認する(コスト試算の実測による裏付け)。

## 残る未確定事項

- 上記の「3回中1回でも不合格なら要改善」という基準自体が暫定であり、実際の生成結果を見た上で
  緩め・厳しめのいずれに調整すべきかは実測後に見直す。
- 同一入力での生成ばらつきをどこまで許容するか(temperature設定等)は、実LLM接続時に
  Claude APIのパラメータ選定と合わせて検討する必要がある(本ドキュメントの範囲外)。
- 検証結果の記録先(本ファイルへの追記か、新規ファイルか)は、実際に検証に着手する段階で
  結果の分量を見て判断する。
