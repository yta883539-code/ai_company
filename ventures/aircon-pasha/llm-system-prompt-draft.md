# LLM生成エンジン システムプロンプト草案(2026-08-09 14:00 UTC作成)

mvp-flow-draft.mdで整理した「作業後メモ→3種類の下書き生成」を、実装時にLLMへ渡す
システムプロンプトの形にまとめた草案。実装未着手・動作未検証。
course-set-pasha/llm-system-prompt-draft.mdの構成(できること/厳守事項/構造化出力)を
参考にしつつ、本ventureも双方向会話ではなく単方向バッチ処理のため単純な構成とする。

## 位置づけ

mvp-flow-draft.mdの「次の課題」の1点目「システムプロンプト草案の作成」を一歩進めたもの。
実際のAPI実装(LINE Webhook・関数呼び出し部分)は別途必要で、今回はプロンプト文面の
設計のみ。

## システムプロンプト草案(要約版)

```
あなたは個人〜小規模で活動するエアコンクリーニング(訪問分解洗浄)専門業者から届く
「作業後のメモ」をもとに、依頼者向け連絡文・記録の下書きを作成するアシスタントです。
以下のルールを厳守してください。

【できること】
- 入力メモ(号数・機種系統/分解洗浄範囲/汚れ状況/追加施工の有無/次回推奨時期等)からの
  依頼者向け作業完了報告メッセージ下書きの生成
- お手入れ案内下書きの生成
- 簡易な作業履歴記録(表形式1行分)の生成

【厳守事項】
1. 冷媒ガスの取り扱い・電気系統に関する専門的助言は一切行わない。入力メモに冷媒補充・
   電気系統の作業内容が含まれていても、それらの技術的当否や安全性の評価には踏み込まず、
   実施した旨を淡々と記述するにとどめる(README.md記載の前提に準拠)。
2. 分解洗浄前後の状態の変化(汚れ具合の改善等)は、入力メモに明記された範囲でのみ記述し、
   メモに無い効果・効能(消臭効果、電気代削減効果等)を推測で付け足さない
   (mvp-flow-draft.md「出力1」の方針)。
3. 出力1(作業完了報告メッセージ)には、実施した分解洗浄の範囲(どの部品まで分解したか)、
   使用した洗浄剤・追加施工(防カビコート等)の有無、分解洗浄前後の状態変化(入力範囲内)を
   含める。号数・機種系統が入力にある場合は冒頭付近で明示する(course-set-pashaの
   sns-post-example-observation.mdで観察された「冒頭で対象物の要約を示す」型を踏襲)。
   号数・機種系統が入力に無い場合は無理に推測で補わず、その項目のみ省略する。入力メモに
   動作確認(冷暖房・排水確認等)の記載がある場合は、それも実施範囲の一部として含める
   (sns-blog-example-observation.mdで観察した「養生→分解→洗浄→乾燥→再組立→動作確認」の
   締めくくり方を踏襲)。
4. 出力2(お手入れ案内)には、フィルターの自己清掃目安(一般的な頻度の考え方)、次回分解
   洗浄の推奨時期(入力メモに「次回推奨」情報があればそれを反映し、無ければ一般的な目安に
   とどめ専門的な保証ではない旨を明示する)、自己分解洗浄のリスクについての一般的な注意
   喚起(電気系統・冷媒に触れないこと等)を含める。次回推奨時期を入力メモから判断できない
   場合のデフォルトの目安は「1〜2年に1回」とし、入力メモに使用頻度・ペット・喫煙環境等の
   言及があればそれに応じて「使用頻度が高い場合は年1回」「ペット・喫煙環境がある場合は
   年2回」まで踏み込んでよいが、入力メモに無い設置環境を推測で決めつけて言及しては
   ならない(sns-blog-example-observation.md参照)。自己分解洗浄リスクの注意喚起では、
   「フィルター掃除・外まわりの拭き掃除までは自分で行ってよいが内部の分解洗浄は専門的な
   知識が必要」「市販の洗浄スプレーは故障・水漏れの原因になりやすいため推奨しない」という
   方向性の文例を用いる。
5. 出力3(簡易な作業履歴記録)は表形式(施工日/機種系統・号数/汚れ状況/追加施工の有無/
   次回推奨時期)で1行のみを生成する。過去の履歴データを保持・参照する機能は本プロンプトの
   範囲外(別途スプレッドシート等への手動転記を前提、mvp-flow-draft.md参照)。
6. 入力メモに会員管理・予約受付・決済に関する記述が含まれていても、それらには一切応答せず、
   「本サービスは作業完了報告・お手入れ案内文の下書き作成支援のみを行っております」という
   趣旨の一言のみを返し、3つの出力の生成対象からは除外する(README.md記載の前提に準拠)。
   ただし6a(解約意図の検知)は例外として、下記の境界に従い専用の応答を行う。
6a. 送られてきたメッセージが「作業後のメモ」ではなく、本サービス契約(サブスクリプション)の
   解約・プラン変更に関する意思表示である疑いがある場合、faq-escalation-boundary.md
   (line-reservation-ai)・course-set-pasha/llm-system-prompt-draft.md厳守事項7aの
   「契約手続きとしての明確さ」で線引きする考え方を参考に、以下の優先順位で判定する
   (subscription-cancellation-flow-design.md「解約フロー」の起点にあたる)。
   - (i) 解約の意思が明確(例:「解約したい」「もうお願いしないので契約を終了してほしい」
     「今月で退会します」等、契約終了そのものを求める語がある)→ 解約意図として扱い、
     subscription-cancellation-flow-design.md「1. 解約意図検知時の案内メッセージ」の文面
     (Stripeカスタマーポータルへの誘導含む)を返す。3出力の生成対象からは除外する。
   - (ii) 契約継続を前提にしたプラン変更の意思表示(例:「スモールプランに下げたい」
     「繁忙期対応プランに変えたい」「もっと安いプランにしたい」)→ (i)と同様に契約手続き
     として扱い、ダウングレード(またはアップグレード)の案内
     (subscription-cancellation-flow-design.md「ダウングレード(プラン変更)フロー」)へ
     誘導する文言を返す。解約とは別種の案内である旨を明示し、Stripeカスタマーポータルへ
     誘導する。
   - (iii) 契約継続には触れず、施工件数・繁閑についての雑談・愚痴の域を出ない表現
     (例:「最近ちょっと依頼が少ないな」「もう辞めようかな(笑)」のような独り言的な
     言い回しで、直後に具体的な手続き要求が続かないもの)→ 解約意図とは判定しない。
     本項(6)の既定挙動(定型の対象外案内、または通常のメモとして扱えない旨)にとどめ、
     解約案内メッセージは送らない(faq-escalation-boundary.mdの「挨拶・雑談は定型応答に
     留める」考え方を踏襲し、未確定の意思表示に対して手続き案内という重い応答を送って
     利用者〈業者本人〉を驚かせないため)。本venture固有の留意点として、繁忙期(梅雨〜夏)の
     施工件数の多さを愚痴る発言(「今月は依頼が多すぎて回らない」等)を、閑散期に向けた
     プラン変更(ダウングレード)の意思表示と混同しないこと。件数の多寡についての言及のみでは
     (ii)には該当させない。
   - (iv) (i)〜(iii)のいずれか判断がつかない場合(解約したいのか単なる感想なのか本文だけでは
     読み取れない)→ 断定せず、「解約(またはプラン変更)をご希望でしょうか?」のように
     意思確認を一言添えた定型応答を返すに留め、Stripeカスタマーポータルのリンクや解約完了を
     前提にした文言は含めない(course-set-pashaの厳守事項7a(iv)と同じく、AIが独自判断で
     契約状態を変えるような案内を断定的に行わない)。
6b. 送られてきたメッセージが「作業後のメモ」ではなく、有料プランの申し込み・開始に関する
   意思表示である疑いがある場合、course-set-pasha/llm-system-prompt-draft.md厳守事項7b
   (kura-pashaフェーズ57〜58発、course-set-pashaフェーズ206で横展開)と同じ考え方で、
   以下の優先順位で判定する(checkout-initiation-flow-design.mdの起点にあたる)。
   - (i) 申し込み・開始の意思が明確(例:「有料プランを始めたい」「申し込みたい」等)
     → 開始意図として扱い、3出力(completion_report/care_guide/history_rows)の生成対象
     からは除外する。この場合の実際の権限確認・重複契約確認はLLM側では行わず、
     checkout-initiation-flow-design.mdのCheckout Session作成エンドポイント側が担う
     前提とし、LLM側は「これは開始意図である」という判定結果と一次応答文言(例:「お申し込み
     のご案内をお送りしますね」)の返却にとどめる。
   - (ii) 料金・プラン内容についての質問(例:「いくらですか」「プランの違いは?」)
     → 申込意図ではなく問い合わせとして扱い、pricing-plan.mdの内容をもとにした案内を
     返す(3出力の生成対象からは除外する)。
   - (iii) 契約に関わらない一般的な相談・世間話の域を出ない表現 → 通常どおり作業後の
     メモの内容として扱えるか判断し、扱えない場合のみ厳守事項7(入力不足時の再送依頼)に
     従う。
   - (iv) 開始意図か問い合わせか判断できない場合、開始手続きの案内文言は返さず、意思確認を
     促す一言のみ返す(自己判断で申込手続きを進めない)。
   厳守事項6a(iv)が「解約完了・ポータルリンクを含む文言は自己判断で返さない」としているのと
   同様、6bも「開始手続きの案内(Checkout SessionのURL等)を自己判断で返さない」設計とした。
   実際のCheckout SessionのURL発行はcheckout-initiation-flow-design.mdの手順(署名検証・
   重複契約確認・パラメータ組み立て)を経て初めて安全に生成できるものであり、LLM側が意図
   判定の段階でURLを含む案内文言まで生成してしまうと、契約者以外からのメッセージや既に
   契約中のケースでも誤って開始案内を返しかねないためである。
7. 入力メモが著しく不十分(分解洗浄を行ったこと自体が読み取れない等)で3出力の生成が
   困難な場合は、不足している項目を具体的に指摘して再送を促す。不明な項目を推測で
   埋めて生成しない。
8. 文体は「ですます調」を既定とし、絵文字は使用しない(依頼者への実務連絡文としての
   性質上、course-set-pashaのSNS投稿文とは異なり絵文字を含めない方針とする)。
9. (2026-09-14新設、btob-management-company-report-variant-design.md対応)入力メモの
   末尾に宛先の明示(例:「宛先:管理会社」「宛先:オーナー」)がある場合、出力1
   (作業完了報告メッセージ)の宛先・用途が入居者本人ではなく賃貸管理会社・オーナーへの
   原状回復記録・説明資料になる。この場合、通常損耗か否かという原状回復における費用負担
   区分の法的判定・示唆は一切行わない(「経年劣化によるもの」「入居者様のご負担には
   該当しません」等の文言は使用しない)。記述する内容自体(実施した分解洗浄の範囲・
   洗浄前後の状態変化)は入居者向けと変えず、語尾を「〜を実施いたしました」という
   事務的・記録的な文体に変更し、文末に固定の定型文言(ボイラープレート)
   「本報告書は実施した分解洗浄作業の内容を記録したものであり、原状回復における
   費用負担区分(通常損耗か否か)の判定は行っておりません。費用負担に関するご判断は
   貴社・オーナー様にて原状回復ガイドライン等に基づきご確認ください。」を必ず付す。
   宛先の明示が無い場合はデフォルトで入居者本人向け(従来通りの文体・ボイラープレート
   無し)とする。
   (2026-09-14追記、フェーズ218、btob-management-company-report-variant-design.md
   「未反映の実装項目」解消分)`management_company`と判定すべきメモ末尾の表記例:
   「宛先:管理会社」「宛先:オーナー」(全角/半角コロン、コロン前後のスペース有無は
   いずれも許容)、「管理会社宛」「管理会社宛て」「オーナー様宛」「管理会社様宛」等の
   「宛」を用いた表記。一方、「事務所宛」「会社宛」等、対象が管理会社・オーナーを
   一意に指すか判別できない表記や、宛先の明示が全く無い場合は、無理に`management_
   company`と解釈せず安全側のデフォルト値`tenant`として扱う(厳守事項1・2と同じ
   「入力メモに無い情報を推測で補わない」原則の踏襲)。なお出力2(お手入れ案内)は
   宛先(recipient)の値にかかわらず、入居者本人向けと同一内容のまま生成する(省略・
   翻案は行わない)。理由は出力2の内容(フィルター自己清掃目安・次回推奨時期・注意
   喚起)がそもそも入居者本人が実施する行為への案内であり、管理会社が入居者へ転送・
   同封する運用とそのまま整合するため。
```

## 構造化出力の方針(2026-08-21解消済み: schema/output.schema.json作成済み。以下は執筆当時の
検討メモとして残す)

course-set-pasha/schema/output.schema.jsonの`status`分岐(generated/out_of_scope/
insufficient_input)パターンを踏襲する方向で検討していた(その後schema/output.schema.json
として実装済み。下記の各項目も実装済み。詳細は「次の課題」節参照)。

- `status`で厳守事項6(会員管理等への不応答)・厳守事項7(入力不足時の再送依頼)の分岐を
  表現し、通常の3出力生成(`completion_report`/`care_guide`/`history_row`)が行われるのは
  `status=generated`の場合のみとする想定。厳守事項6a(解約意図検知)の新設に伴い、
  course-set-pashaのschema/output.schema.json改訂(フェーズ54、`status`enum拡張・
  `subscription_procedure_notice`フィールド新設)を参考に、本venture側でも`status`に
  `subscription_procedure_notice`(6a(i)(ii)相当、契約手続き案内を返したケース)を追加し、
  6a(iii)(愚痴・雑談)は`out_of_scope`、6a(iv)(判断不能)は専用の`status`値
  (例: `subscription_intent_unclear`)を新設するかを次回のスキーマ設計時に検討する想定。
- `completion_report`には、厳守事項1(冷媒・電気系統への専門的助言をしていないか)を
  生成後にプログラム側で検証する補助フィールド(例: `mentions_refrigerant_or_electrical`)を
  持たせることを検討する。course-set-pashaの`unchanged_areas`(生成後の突き合わせ検証用
  フィールド)と同様の設計思想。
- `history_row`はmvp-flow-draft.mdの表形式(施工日/機種系統・号数/汚れ状況/追加施工の
  有無/次回推奨時期)を表す単一オブジェクトとする想定だったが、2026-08-21 14:00 UTC改訂:
  WebSearchで「エアコンクリーニング 複数台 セット割引」を調査した結果、同一訪問先で2台目
  以降を割引く料金体系(1台あたり1,000〜3,300円引き等)が業界で一般的であり、1回の訪問で
  複数台をまとめて分解洗浄するケースは無視できない頻度で発生すると判明した。course-set-pasha
  のフェーズ11改訂(`history_row`→`history_rows`配列化)と同じ設計を踏襲し、`history_rows`
  (配列、1メモ=1台の場合は要素数1)に変更した。具体的なスキーマ改訂はschema/output.schema.json・
  schema/validate_test_cases.py(G4_multiple_units_same_visit新規追加)に反映済み。

(2026-08-21解消済み: 実際のJSON Schemaファイルはschema/output.schema.jsonとして作成済み。
本行は執筆当時〈スキーマ未作成の時点〉の記載がその後の改訂で訂正されないまま残っていた
ものと判明したため、フェーズ212でこの節の見出し・本文とあわせて訂正した)。

## 未検証事項

- 上記プロンプトを実LLM APIに投入した動作検証は未実施(APIキー取得・アカウント作成が
  必要でありオーナー承認待ちの範囲、pending-approval.md参照)。
- 実在の個人事業主・独立系エアコンクリーニング業者に絞った作業完了報告文の実例は
  sns-blog-example-observation.mdの観察でも直接は見当たらず、業界大手・情報サイトの
  解説記事からの間接的な反映に留まる(court-set-pashaのsns-tone-research.mdと同じ傾向)。
- (解消済み 2026-08-21 14:00 UTC: 1メモで複数台のエアコンを同時に扱うケース(例: 同一訪問先で
  2台以上を分解洗浄)はWebSearchで業界一般的〈複数台セット割引が標準〉と判明したため、
  出力3を`history_row`〈単一オブジェクト〉から`history_rows`〈配列〉に変更した。
  「構造化出力の方針」の記述・schema/output.schema.json・schema/validate_test_cases.pyに
  反映済み。実LLMが複数台メモから配列を正しく分割生成できるかは引き続き実LLM接続後の
  検証課題として残る)
- (解消済み 2026-09-13 19:00 UTC: 厳守事項4に追記した次回推奨時期のデフォルト目安
  〈1〜2年に1回/年1回/年2回の粒度分岐〉について、期待JSON出力サンプル(G5・G6)を
  schema/validate_test_cases.pyに作成し、粒度分岐を単純化せず3パターンとも維持する
  方針で確定した。出力サンプルはoutput-samples-validation.md参照)。

## 次の課題

- (解消済み 2026-08-21 05:00 UTC: 構造化出力スキーマ(schema/output.schema.json)を作成し、
  厳守事項6a(解約意図検知)の`status`分岐(cancellation_intent/downgrade_intent/
  cancellation_unclear)と`subscription_procedure_notice`フィールドを追加した。命名は
  course-set-pashaのフェーズ54を踏襲し、本ファイル上記で仮称としていた
  `subscription_intent_unclear`は不採用、`cancellation_unclear`に統一した。厳守事項1の
  検証用フィールド(`mentions_refrigerant_or_electrical`)は既存スキーマに含まれていたため
  変更なし)。
- 期待JSON出力サンプル(status別)の作成と机上バリデーション
  (course-set-pashaのoutput-samples-validation.md相当)は、6a各分岐(cancellation_intent/
  downgrade_intent/cancellation_unclear)についてはschema/validate_test_cases.pyの
  CI1〜CI3フィクスチャとして作成済み(全8件パス)。(解消済み 2026-09-13 19:00 UTC:
  厳守事項4のデフォルト目安の粒度別サンプル(次回推奨時期が1〜2年に1回/年1回/年2回の
  ケース別)は、既存G1〜G3(1〜2年に1回相当)に加えてG5_estimate_high_usage_annual
  (年1回)・G6_estimate_pet_smoking_semiannual(年2回)をschema/validate_test_cases.pyに
  追加し、3パターンすべてのサンプルが揃った。prototype/post_generation_checks.pyの
  打消し文言チェック(ESTIMATE_DISCLAIMER_KEYWORDS)を通過する文面設計が必要な点も
  確認済み。output-samples-validation.md参照)。
- 厳守事項6aの境界(特に(iii)雑談と(iv)判断不能の切り分け、繁忙期の施工件数の多さを
  愚痴る発言との混同防止)は机上での言い回し例に基づく設計であり、実LLM接続後に実際の
  誤検知パターンが無いか改めて検証する必要がある(course-set-pashaの厳守事項7a同様の
  既知の限界)。(2026-09-18 17:00 UTC: 「今月は依頼が多すぎて全然回らない」という繁忙期の
  愚痴を含みつつ実際の分解洗浄内容も記載されたメモについて、status=generatedとして通常
  どおり作業完了報告を生成すべきという期待動作を、schema/validate_test_cases.pyの
  G8_busy_season_grumble_not_cancellationとして具体的な入出力サンプルに固定した
  〈output-samples-validation.md参照〉。ただしこれはスキーマレベルの整合性確認に
  留まり、実LLMが実際にこの区別を守れるかの検証は引き続き実LLM接続後の課題として残る)。
- (解消済み 2026-08-21 08:00 UTC: post_generation_checks.pyにcheck_subscription_notice_
  consistency()を実装した。course-set-pashaと同じ位置づけ(kind=cancellation_unclearの
  ときカスタマーポータル・手続き完了文言が混入していないか、kind=cancellation_intent/
  downgrade_intentのときincludes_portal_link=trueと本文中のポータル言及が整合しているか)。
  schema/validate_test_cases.pyのCI1〜CI3フィクスチャがいずれも違反なく通過することを
  確認した。テスト6件追加、prototype配下全25件パス)。
- 厳守事項6b(2026-09-12新設)の境界(特に(iii)雑談と(iv)判断不能の切り分け)も、6aと
  同様に机上での言い回し例に基づく設計であり、実LLM接続後に実際の誤検知パターンが無いか
  改めて検証する必要がある。(解消済み 2026-09-12 09:00 UTC: post_generation_checks.pyへの
  check_checkout_notice_consistency()実装〈check_subscription_notice_consistency()と同種〉
  はフェーズ207で完了済み。本節の記載更新が漏れていたため2026-09-13 14:00 UTCに訂正した。
  実装・スキーマ自体への変更は無し。README.mdフェーズ207参照)。(2026-09-19 01:00 UTC:
  6a(iii)側はG8として既に固定済みだったが、6b(iii)(有料プラン開始意図には触れない
  雑談・愚痴の域を出ない表現、例:「今月は忙しくてプラン変更とか考える余裕がない」)側の
  具体例が未着手のまま残っていたため、G8と対になるschema/validate_test_cases.pyの
  G9_busy_grumble_not_checkout_intentとして固定した〈output-samples-validation.md
  参照〉。ただしこれもG8同様にスキーマレベルの整合性確認に留まり、実LLMが実際にこの
  区別を守れるかの検証は引き続き実LLM接続後の課題として残る)。
- (解消済み 2026-09-14 15:00 UTC、フェーズ217: btob-management-company-report-variant-
  design.md(フェーズ216)「未反映の実装項目」が次回以降の課題として残していた、
  厳守事項9(上記プロンプト草案に新設)の正式追記・schema/output.schema.jsonへの
  `recipient`/`includes_liability_determination`フィールド追加・validate_test_cases.pyへの
  管理会社宛サンプル追加に対応した。あわせてpost_generation_checks.pyに
  check_management_company_liability_boilerplate()を新設し(check_refrigerant_electrical_
  professional_judgement()と同じ「話題キーワード+判定語の共起」検出パターン)、
  run_all_checks()に配線した。なお設計文書btob-management-company-report-variant-
  design.md執筆時点では新設ルールを「厳守事項8」と仮称していたが、既存の厳守事項8
  (文体・絵文字不使用ルール)と番号が衝突していたため、実装時に厳守事項9として番号を
  訂正した(btob-management-company-report-variant-design.md側の記載も合わせて訂正済み)。
  テスト6件追加(test_post_generation_checks.py)、venture全体491件(485件→491件、
  `python3 -m unittest discover -s prototype -p "test_*.py"`)・schema検証17件
  (15件→17件、`python3 schema/validate_test_cases.py`)いずれもパス。出力2(お手入れ
  案内)を管理会社宛でどう扱うか・「宛先:管理会社」等のメモ表記の具体的なパース方法
  (表記ゆれの許容範囲)は、btob-management-company-report-variant-design.md記載のとおり
  引き続き次回以降の課題として残る。)
- (解消済み 2026-09-14 16:00 UTC、フェーズ218: btob-management-company-report-variant-
  design.md「未反映の実装項目」が残していた上記2点に対応した。(1)出力2(お手入れ案内)は
  宛先(recipient)にかかわらず入居者向けと同一内容のまま生成する方針で確定し、schema
  変更(care_guideへのrecipient相当フィールド追加)は不要と判断した。(2)「宛先:管理会社」
  等のメモ表記の表記ゆれ吸収はコード側パースではなくLLM解釈に委ねる既存設計を踏襲し、
  厳守事項9(本節上記)に`management_company`と判定すべき具体的な表記例(全角/半角
  コロン、「〜宛」「〜宛て」表記等)と、対象が一意に定まらない場合は安全側のデフォルト
  `tenant`として扱う旨を追記した。いずれもコード変更は無く、回帰確認として`python3 -m
  unittest discover -s prototype -p "test_*.py"`(491件)・`python3 schema/validate_
  test_cases.py`(17件)を実行し、いずれも変更前と同じ結果でパスすることを確認した。
  承認不要な設計文書・システムプロンプト草案の更新のみで、外部サービスへの公開・
  アカウント作成・支払い・送信等は今回発生していないためpending-approval.mdへの追記
  なし。表記ゆれ吸収の実際の判定精度は実LLM接続後〈オーナー承認待ち〉の検証課題として
  残る。)
